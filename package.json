{
  "name": "notebooklm-remover-replica",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run prepare:ffmpeg && vite",
    "build": "npm run prepare:ffmpeg && vite build",
    "preview": "vite preview",
    "prepare:ffmpeg": "node scripts/prepare-ffmpeg-local.mjs"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@ffmpeg/core": "0.12.10"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^5.4.11"
  }
}
